version: '3'

services:
  front:
    build: site-e-commerce
    ports:
      - "80:80"

  api:
    build: api
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    volumes:
      - ./api:/api

